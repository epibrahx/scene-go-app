# ROADMAP.md · SceneGo 产品文档（唯一）

> **docs/ 唯一产品文档**（2026-08-06 合并 ROADMAP + PLAN；UI 形态文档已随 UI 删除）。
> 完成状态以本文件为准；已完成的能力由 git 版本管理记录，不在此维护。

## ⚠️ 给所有 Agent 的声明（必读）

1. **查进度**：看本文件「正在做的」；查已完成的能力 → 看 git 历史。
2. **改进度**：开始做 → 加入「正在做的」；做完 → 从本文件删除并提交（git 即记录）。
3. **状态图例**：🔄 进行中 · 🔶 部分完成。
4. **维护节奏**：开始/完成时更新，不设模糊的"推进中"。

---

## 一、产品概述

面向**全球出境用户**：**用户主动触发**（拍照 / 语音 / 文字），App 识别当前场景并生成**符合该场景的当地语言表达卡**，让用户能立即把需求/情境有效地传达给当地人（司机、店员、警察、海关等）。

数据基础：26 国待核验安全候选信息。核心承诺：**秒级成卡、精准表达**。

> 2026-08-06：当前 UI 形态已整体删除（保留功能内核），UI 待重新设计后实现下列能力。
> 2026-08-11：公开就绪阶段的 9 屏 UI（src/screens|components|theme|app）已全部删除，App 为空壳（仅功能内核初始化）。新 UI 完全基于 docs/reference/DESIGN-v2.1.pen（SCN-27 定稿）重建。

## 二、正在做的

## 三、功能规划（未开始）

## 四、暂不规划（一句话）

## 五、里程碑

---

## 版本关系

第一版概念草图 → 第二版对话式 UI（已删除）→ 2026-08-06 功能内核（UI 待重新设计）→ 2026-08-11 公开就绪 UI 删除（空壳，基于 DESIGN-v2.1.pen 重建）→ 远期构思（未动）

## 维护示例（给 Agent 的操作范式）

```
场景：某能力开始做 / 做完
动作：
  1. 开始做 → 加入「二、正在做的」；做完 → 从该表删除并提交（git 记录完成）
  2. 删除被替代的过程文档（git 历史可查）
  3. 提交信息遵循 Conventional Commits：docs(roadmap): XXX 完成
```
